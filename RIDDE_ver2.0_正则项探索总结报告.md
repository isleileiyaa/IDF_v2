# RIDDE Training Objective ver2.0 — 正则项消融探索总结报告

日期:2026-08-11 ~ 2026-08-16
范围:`idf_ridde_v2`(ChronosBoltRetrieve,backbone 冻结),`L = L_pred + ρ_sem·L_sem + ρ_xcov·L_xcov + ρ_ord·L_ord`(论文 Eq.15)及两个衍生正则项 `L_cos`/`L_gbal`
详细数据见四份分报告:[sem](RIDDE_ver2.0_sem消融实验报告.md) / [xcov](RIDDE_ver2.0_xcov消融实验报告.md) / [cos_gbal](RIDDE_ver2.0_cos_gbal消融实验报告.md) / [ord](RIDDE_ver2.0_ord消融实验报告.md)

## 一句话总结

**论文提出的三个正则项(sem/xcov/ord)没有一个是"开箱即用"的——sem 基本不起作用,xcov 和 ord 都通过"钻空子"的退化解达成了字面目标但伤害了准确率;我们在过程中意外发现的 `L_cos`(其实等价于 RIDDE ver1.0 已有的正交项)是目前唯一干净、可靠、经过多种子验证的正向结果。** 论文最初想要的"一支平稳、一支震荡"的可视化效果,到目前为止没有任何配置(单独或组合)真正实现过。

## 测试路径(按时间顺序)

```
baseline(纯L_pred)
  → 阶段2: L_sem 单测(0.01/0.1/0.2/0.3/0.5/0.7/1,含3种子校验)
  → 阶段3: L_xcov 单测(0.01/0.1/1,含3种子校验)
  → [诊断三件套] y_inv/y_dyn可视化、cos_sim正交性数学分析、gamma饱和度
  → 针对xcov问题提出并测试: L_cos 单测、L_gbal 单测、L_cos+L_gbal 合并
  → 回头验证: RIDDE ver1.0 的 L_dis 是否和 L_cos 效果一致(重新按同协议跑)
  → 阶段4: L_ord 单测(0.01/0.1/1)
  → L_cos + L_ord 合并
  → 回头重新核对论文原文(nips_2026_TS.pdf),发现论文明确要求 L_ord 配"小权重",原测试区间可能偏大
  → 补测 L_ord 小权重区间(0.0001/0.001/0.01),重写 L_ord 报告
```

每个正则项都遵循同一套协议:`train_steps=10000`,ETTh1/ETTh2/ETTm1/ETTm2/weather/exchange_rate 六个数据集,先单独测,确认有信号后再考虑组合;凡是数值上"看起来变好"的结果,都补了 2-3 个随机种子做方差校验,不轻易采信单种子结果。

## 面临的主要问题

### 1. 检索置信度 `c_i` 经常接近 0,悄悄阉割了正则项的梯度

`L_sem`、`L_ord` 都用 `c_i`(检索一致性置信度)给每个样本的损失加权。实测发现 `c_i` 在 ETTh2/ETTm2/exchange_rate 上均值几乎精确为 0,只有 ETTh1(~0.02)、ETTm1(~0.12)、weather(~0.003)有非零值。这直接解释了 `L_sem` 在安全权重范围内(0.01~0.3)几乎测不出任何效果的原因——不是正则项设计得没用,而是它在这批数据下几乎没有被真正触发过。`L_ord` 情况不同:虽然诊断时同一批 `c_i` 也很低,但它的效果依然显著地体现了出来(而且是负面的),说明训练阶段(用的是更大的混合语料)存在足够多高 `c_i` 样本把约束"烙"进了共享权重里,不能一概而论。

### 2. 两个正则项都找到了"退化解"——达成字面目标,但用的是投机取巧的方式

这是本轮探索里最重要的发现,`L_xcov` 和 `L_ord` 是同一类问题的两个变体:

- **`L_xcov`(批次协方差形式)**:测的是 `z_inv`/`z_dyn` 在一个 batch 内跨样本是否一起变化。模型发现只要让 `z_inv` 对所有样本都输出同一个(接近 0 的)常量,批次内协方差就自动趋于 0——根本不需要学会让两支携带不同语义。实测 gamma 均值从 0.516 塌缩到 0.008~0.02,`energy_share_inv`(y_inv 分支的信息占比)从 45% 掉到 0.3~0.7%,`y_inv` 分支事实上被关闭了。
- **`L_ord`(粗糙度排序)**:要求 `y_inv` 比 `y_dyn` 更平滑。模型发现让 `y_inv` 方差变小(不是学出有意义的平滑趋势,就是单纯压低输出幅度)就能让"粗糙度"这个统计量变小。实测 `rho_ord≥0.1` 时 gamma 均值掉到 0.393,`energy_share_inv` 掉到 35%,程度比 xcov 轻,但方向完全一样,且是随权重单调、可重复的确定性效应(不需要种子校验也能确认)。

两者的共同教训是:**给一个"应该产生某种统计特征"的正则项设计损失函数时,如果损失函数本身可以通过"让某一支归零/趋同"来投机取巧地满足,模型就一定会走这条捷径**,而不是学习设计者真正想要的、更难学的"两支都保留信息但呈现出不同统计特征"。

### 3. 直接惩罚"样本内"关系(而不是"批次统计")能避免这个坑

针对问题2,我们提出并验证了改用样本内的 `cos_sim(z_inv, z_dyn)` 直接做正则(`L_cos`),而不是批次协方差。结果是目前测过的所有正则项里最健康的一个:cos_sim 真正降到接近 0(而不是像 xcov 那样"降不动"甚至不降反升),同时 `energy_share_inv` 全程稳定在 45%~46%,两支都在被使用,MSE 平均还有 -1.46% 的提升(exchange_rate -8.5%,经种子校验\*)。这个发现也回答了一个额外的问题:`L_cos` 在数学上跟 RIDDE **ver1.0 已有的 `L_dis`**(`idf_clean_dis`,`dis_mode='latent'`)完全等价——归一化后点积就是 cos_sim。用跟本轮完全一致的协议重新跑了一遍 ver1.0 的 `L_dis`,诊断曲线(gamma/cos_sim/energy_share)和 `L_cos` 逐点吻合,证明这不是巧合,是同一个机制在两版架构里的复现。

\*成功案例仍有边界:`L_gbal`(专门设计用来防止 gamma 塌缩)单独测试时完全无效——因为 baseline 的 gamma 均值本来就接近 0.5,没有"敌人"可以防;跟 `L_cos` 合并后不仅没有额外收益,反而把 -1.46% 的收益拖到了 -0.08%。防御性正则项只有在真的存在塌缩风险时(比如配合 `L_xcov`)才有意义,这个组合还没测过。

### 4. 组合能部分缓解退化解,但代价是"各打五十大板"

`L_cos + L_ord` 的合并测试证明了"先解耦、再雕刻形状"的思路是对的方向:gamma 均值从 ord 单独时的 0.393 回升到 0.451,`energy_share_inv` 从 0.351 回升到 0.424,MSE 从单独用 ord 的 +1.94%(全面变差)降到接近中性的 +0.35%。但三个指标都没有完全恢复到 `L_cos` 单独使用时的健康水平——`L_ord` 依然在部分地啃 `y_inv` 的能量,只是被 `L_cos` 的约束拦住了一部分,而且这个组合吃掉了 `L_cos` 单独使用时的准确率收益。**没有免费的午餐**:目前没有找到一个配置能同时拿到"cos 的准确率收益"和"ord 的粗糙度分离"。

### 5. 单种子结果不可信,exchange_rate 尤其坑

exchange_rate 这个数据集的种子间波动特别大(std 能到 0.003~0.008,相当于均值的 3%~10%)。`L_sem` 阶段一开始看到的"exchange_rate 在 sem=0.01/0.1 下变好 -3.6%"的信号,补了 3 个种子后完全消失,甚至反转成变差。这个教训贯穿了整个探索过程——之后每一个看起来"有效"的数值结果(`xcov=1`、`cos=1`),都补做了种子校验才敢下结论,而 `ord` 和它的合并测试目前还只有单种子结果,如果要作为最终结论使用,也需要补种子。

### 6. "gamma 饱和度高"本身不是问题,但很容易被误读

探索过程中一度把"gamma 值都接近 0/1"当作需要修复的 bug(这也是最初驱动这轮实验的动机之一)。深入分析后发现,gamma 饱和本身是模型对每个特征维度做"归属决策"的正常表现,真正该看的指标是 **`energy_share_inv = Var(y_inv) / (Var(y_inv) + Var(y_dyn))`**(不变分支的输出方差占两支总方差的比例,直觉上就是"y_inv 分支携带的信息量占比")——只要它保持在合理区间(30%~70%),gamma 饱和度再高都不算坏事;`energy_share_inv` 塌缩到接近 0% 或 100% 才是真正的问题信号(前者如 `L_xcov`,后者尚未在本轮实验里观察到)。`cos_sim` 也只是"两支是否携带不同信息"的一个数学近似代理,不是必须死磕到 0 的硬指标。四份分报告(尤其 sem、ord)最初撰写时遗漏了这个指标的正式定义,后续已经补上。

### 7. 没有一个配置实现了最初想要的"可视化上一平一糙"

这是贯穿全程、始终没有解决的问题。不管是 `L_ord` 单独、还是 `L_cos+L_ord` 组合,`R(y_dyn)-R(y_inv)` 这个统计量都能被拉开(最高到 0.76~0.84,换算成方差比是 2~2.3 倍),但把 `y_inv`/`y_dyn` 曲线画出来看,肉眼依然分不出哪支更平稳——两支看起来都是同量级的锯齿噪声。粗糙度差目前只是一个统计显著、但视觉不可辨的量,论文插图级别的清晰分离没有在任何一组实验里出现过。

### 8. 重新核对论文原文后,发现 `L_ord` 权重测得偏大——但补测小权重后发现的是另一个问题

用户提出质疑后,重新逐条核对了 `nips_2026_TS.pdf` 原文(§3.4-3.6, Eq.1-23)跟代码实现。绝大部分公式(`c_i`、`L_sem`、`L_ord`、`R(ŷ)`、门控/分解/融合架构)都是忠实的,但发现论文原文第 240-241 行**明确要求 `L_ord` 配"相对其他项更小的权重"**,而我们最初测的 `rho_ord ∈ {0.01, 0.1, 1}` 跟 `L_sem`/`L_xcov` 用了同一套尺度,没有遵守这条设计说明。

补测了 `rho_ord ∈ {0.0001, 0.001, 0.01}` 后发现:**论文这条建议在"避免重度退化解"这件事上是对的(小权重确实基本避开了 gamma/energy_share 塌缩)**,但单种子结果本身很不稳定(`0.0001` 看起来"变好"-0.67%,紧邻它的 `0.001` 却暴涨到 +3.01%)。补了 2-3 个种子校验后真相浮出水面:**三个小权重的 3 种子平均 MSE 变化分别是 +0.75%/+0.78%/+0.64%——彼此几乎一样,没有随权重变化的趋势,而且没有一个数据集在任何权重下真正变好过。** 也就是说,单种子看到的"忽好忽坏"确实是 exchange_rate 噪声(该数据集种子间标准差可达均值的 6%~10%),但噪声掩盖的不是"其实无害",而是"其实一直在小幅拖累"。**`L_ord` 在论文建议的整个小权重区间内都没有正向效果,只是伤害程度比大权重轻得多。** 这个发现同时也说明:另外两个正则项(`L_sem`/`L_xcov`)的论文公式本身核对下来是忠实的,`L_xcov` 的退化解不是实现错误,是公式本身(批次协方差在小样本/单批次下容易被"z_inv趋同"钻空子)的固有风险,权重大小不能解决这个结构性问题(这点和 `L_ord` 不同——`L_ord` 的重度塌缩是"权重超过阈值才发生",小权重下只是轻度、一致的拖累;`L_xcov` 在测过的所有权重下机制上都不健康)。

## 全部配置一览(6数据集平均 MSE 相对 baseline 的变化)

| 配置 | 平均变化 | 机制健康度(energy_share_inv) | 种子校验 |
|---|---|---|---|
| baseline | — | 45.3% | — |
| L_sem(0.01~0.3,安全区间) | 基本无效(±1%内) | 未受影响 | ✅ 3种子,确认无效 |
| L_sem(≥0.5) | 明显变差(最差+40%) | 未系统测 | 单种子(已足够确认有害) |
| L_xcov(0.01/0.1) | 基本中性 | 轻微下降 | 部分校验 |
| **L_xcov(1)** | **-1.84%(exchange_rate -8.3%)** | **0.3~0.7%(严重塌缩)** | ✅ 3种子,数值可信但机制退化 |
| **L_cos(1)** | **-1.46%(exchange_rate -8.5%)** | **46.2%(健康)** | 单种子,机制诊断充分支持 |
| L_gbal(任意权重) | 基本无效 | 未受影响 | 单种子,机制上可解释为"无用武之地" |
| L_cos+L_gbal(1+1) | -0.08%(收益被抵消) | 46.4%(健康但无额外收益) | 单种子 |
| ver1.0 L_dis(=L_cos 等价物) | 与 L_cos 同量级 | 与 L_cos 逐点吻合 | 单种子,诊断确认机制一致 |
| L_ord(0.0001~0.01,小权重区间) | **小幅但一致变差(+0.64%~+0.78%)** | 44.3~46.5%(基本健康) | ✅ 3种子,确认轻度负面 |
| L_ord(≥0.1) | **确定性变差(最差+2.9%)** | **35.1%(明显塌缩,单调可重复)** | 单种子(效应量级已远超噪声,可信) |
| L_cos+L_ord(1+1) | +0.35%(接近中性) | 42.4%(部分恢复) | 单种子 |

## 建议的下一步(供参考,不代表已经决定)

1. `L_cos`(≡ ver1.0 `L_dis`)是目前唯一经过充分验证的正向结果,如果要往论文的方向继续推进,大概率应该以它为基础,而不是原始论文的 `L_xcov`/`L_ord` 公式。
2. `L_xcov` 的退化解是公式本身的结构性风险(批次协方差在任何权重下都可能被"z_inv趋同"钻空子),`L_ord` 的退化解则随权重连续存在(小权重轻度拖累、大权重重度塌缩,没有真正"安全"的权重)——如果还想保留这两个数学形式,都需要重新设计损失函数本身(比如给 `L_xcov` 换成样本内版本、给 `L_ord` 换成不依赖压低方差就能满足的形式),而不是指望调权重解决。
3. `L_ord` 全部测过的权重(0.0001~1)都已经过种子校验或效应量级本身可信,**结论已经明确:没有找到任何一个权重让 `L_ord` 产生正向效果**。
4. "一平一糙"的可视化目标还没实现,如果这是必须交付的成果,可能需要更直接的约束方式(比如显式惩罚 `y_inv` 的绝对方差上限+下限区间,而不是相对 `y_dyn` 的排序损失)。
5. `L_cos`、`L_ord`(全部区间)结果已经过种子校验或效应量级本身可信;`L_cos+L_ord` 合并结果、`L_xcov` 低权重区间还只有单种子数据,如果要写进正式结论,建议依次补齐。

## 数据与代码索引

- 代码改动:`TS-RAG/models/ChronosBolt.py`(`idf_ridde_v2` 的 loss 块,新增 `loss_sem/loss_xcov/loss_ord/loss_cos/loss_gbal`)、`TS-RAG/pretrain.py`、`TS-RAG/zeroshot.py`、`TS-RAG/script/pretrain_idf_ridde_v2.sh`、`TS-RAG/script/zeroshot_chronos_idf_ridde_v2.sh`
- 诊断脚本:`TS-RAG/ridde_v2_diagnostics.py`(ver2.0 checkpoint 用)、`TS-RAG/v1_dis_diagnostics.py`(ver1.0 `idf_clean_dis` checkpoint 用,hook 方式抓取 gamma/z_inv/z_dyn)
- 全部训练/评估/诊断 log:`TS-RAG/results/ridde_v2_ablation_logs/`
- 全部 y_inv/y_dyn 可视化图:`TS-RAG/results/ridde_v2_diagnostics_plots/`
- 全部逐样本诊断数组:`TS-RAG/results/ridde_v2_diagnostics_npz/`
