# RIDDE 新正则项消融实验清单（v3，公式以论文稿为准）

依据：师姐《RIDDE_Regularization_Design》技术说明（阶段划分和诊断指标）+ 论文稿 nips_2026_TS.pdf 第3.3/3.5节（具体公式，三处矛盾点已确认**按论文来**：融合方式、L_xcov、roughness ratio 都用论文版本，不用设计文档版本）。

设计核心：不变–动态分解 = "检索共识 – 查询残差"分解。三个正则项分工——
- **L_sem（语义锚定）**：用检索共识 ȳ_ret 和残差 r 给 y_inv / y_dyn 定语义目标，文档强调这是**最关键**的正则项
- **L_xcov（批次级去相关）**：降低 z_inv / z_dyn 的统计冗余（比逐样本正交更不容易靠 gamma 饱和"作弊"）
- **L_ord（相对平滑排序）**：约束 dynamic 比 invariant 粗糙至少一个 margin，权重要小

原则不变：**逐个加，前一个验证有效才加下一个**；每个阶段至少覆盖3-4个特征不同的数据集（建议 ETTh1、ETTh2、Exchange、Weather）。

---

## 融合方式：按论文稿，不改成加性融合

已确认不采用设计文档的加性融合建议，维持论文 Eq. (11) 的融合解码器：

    ŷ^q = f_g([ŷ^inv; ŷ^dyn])

后续所有阶段的 L_pred 都基于这个 f_g 融合后的 ŷ^q 计算，不再单独消融"加性 vs. f_g"这一项。

---

## 阶段 1（预测基线）

配置：只用 L_pred = ‖ŷ^q − y^q‖²₂ ，其中 ŷ^q = f_g([ŷ^inv; ŷ^dyn])（论文 Eq. 11/16）

- [ ] 数据集：ETTh1 / ETTh2 / Exchange / Weather
- [ ] 记录三项监测的基线数值/图像，作为后续对比基准
- [ ] 额外基线：旧版 L = L_pred + ρ·L_dis（正交正则），留作对照，方便和新方案比效果

## 阶段 2（语义锚定）：L_pred + ρ_sem·L_sem

前提：阶段1完成，基线数据到手

- [ ] 同样4个数据集
- [ ] 监测1 —— y_inv / y_dyn 可视化：y_inv 是否更平稳、y_dyn 是否承接更多波动
- [ ] 监测2 —— z_inv / z_dyn 余弦相似度：记录数值
  - 接近0（正交）→ 标记"暂不需要额外正交正则"
  - 不接近0 → 记下实际范围，留给阶段3观察 L_xcov 能不能把它拉下去
- [ ] 监测3 —— gamma 分布：是否还扎堆在0/1附近
- [ ] 新增诊断（按设计文档要求）：
  - [ ] 两条分支的 roughness ratio R(ŷ_inv)、R(ŷ_dyn)
  - [ ] 分支输出能量占比（ŷ_inv 和 ŷ_dyn 各自贡献了最终预测的多少能量/方差）
  - [ ] 按检索一致度 c_i 分层看效果（c_i 高 vs. 低的样本子集分别算 MSE/MAE，验证语义锚定在检索可靠时约束更强这条设计逻辑）
- [ ] 判定：MSE/MAE 是否在多数数据集上稳定提升？有效→进阶段3；无效→找师姐讨论

实现细节：L_sem 按论文 Eq. (20) 用 1/M（对训练集/当前epoch的样本求和）做归一化，不用设计文档的 1/B（按单batch归一化）。

## 阶段 3（表示去相关）：阶段2 + ρ_xcov·L_xcov

- [ ] **实现前先确认一个点**：论文 Eq. (21) 自身对 M_b 的定义前后矛盾（公式上方文字说"M_b 是一个batch内的样本数"，公式下方又说"M_b denotes the number of batches"），双重求和 ΣΣ_{j=1}^{Mb} Σ_{i=1}^{B} 具体怎么循环需要师姐拍板。默认按标准训练循环理解：B=当前mini-batch大小，只在当前batch内算交叉协方差（不跨batch累积），即退化成单层求和 (1/(B−1))‖(Z^inv−mean)ᵀ(Z^dyn−mean)‖²_F，除非师姐明确要做跨batch的统计量累积（那需要额外设计一个跨batch的可微缓存机制）
- [ ] 按论文文字，只做**按维中心化**（减均值），不做设计文档建议的标准化（除以标准差）——如果实际训练中发现大尺度维度主导损失，再回头跟师姐讨论要不要加标准化
- [ ] 同样4个数据集
- [ ] 监测1：y_inv/y_dyn 可视化，对比阶段2是否有进一步改善
- [ ] 监测2：z_inv/z_dyn 余弦相似度，是否比阶段2更接近正交（L_xcov 的目标就是替代显式正交约束，理论上应该更低）
- [ ] 监测3：gamma 分布是否比阶段2更分散、不再靠饱和"偷懒"
- [ ] 记录批次级交叉协方差数值本身（不只看余弦相似度，文档建议直接报告这个诊断量）
- [ ] 判定：是否比阶段2进一步提升，且多数据集稳定？有效→进阶段4；无效→讨论

## 阶段 4（行为排序）：阶段3 + ρ_ord·L_ord（权重要小）

- [ ] 全部7个数据集（ETTh1/ETTh2/ETTm1/ETTm2/Weather/Electricity/Exchange），凑齐论文Table
- [ ] 监测1：y_dyn 是否明显比 y_inv 粗糙——按论文 Eq. (23) 用 R(ŷ)=log(Var(Δ²ŷ)+ε)（二阶差分、取log方差，**不除以Var(ŷ)**，不用设计文档那版"除以Var(ŷ)归一化"的写法），验证零损失条件 R(ŷ_dyn) ≥ R(ŷ_inv) + margin 是否满足；L_ord 求和同样按论文用 1/M 归一化
- [ ] 监测2：z_inv/z_dyn 余弦相似度最终结果——决定最终版是否还要额外加正交正则项
- [ ] 监测3：gamma 分布最终结果，确认路由门控饱和问题解决
- [ ] 新增诊断：不同分布偏移强度下的预测性能（比如按测试集与训练集分布差异分档，看性能是否稳健，这是判断提升是否真的来自 invariant-dynamic 分工、而不是分支坍缩的关键证据）
- [ ] 判定：与阶段1/2/3逐级对比，写进消融表

---

## 收尾

- [ ] 把每阶段的诊断结果（可视化、余弦相似度、gamma分布、roughness ratio、能量占比、分布偏移鲁棒性）整理成对比图表，用于论文消融实验
- [ ] Table 4 和消融讨论文字目前还是旧的 "w/o L_dis" 表述，测完要同步改成新命名（w/o L_sem / w/o L_xcov / w/o L_ord）
- [ ] 如果阶段4显示 z_inv/z_dyn 天然接近正交，补一组"+显式正交正则"对照看有没有增量收益；没有的话就不加，论文里写明 L_xcov 已经足够
- [ ] 写作提醒（设计文档第8节）：方法部分表述要说清楚这是"retrieval-guided inductive bias / operational decomposition"，不要写成严格的 identifiability 保证——避免审稿人抓这个理论边界的漏洞
